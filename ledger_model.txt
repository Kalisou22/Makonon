<?php
namespace App\Models;

use Illuminate\Database\Eloquent\Model;

class Ledger extends Model
{
    public $timestamps = false;
    protected $table = 'ledger';
    protected $fillable = ['agence_id', 'transfert_id', 'type', 'nature', 'montant', 'solde_avant', 'solde_apres', 'utilisateur_id'];
    
# ============================================================
# SCRIPT COMPLET - CRÉATION DE TOUS LES FICHIERS
# EXÉCUTER DANS GIT BASH DEPUIS /c/xampp/htdocs/makonon-api
# ============================================================

echo "=========================================="
echo "MAKONON TRANSFERT API - CRÉATION DES FICHIERS"
echo "=========================================="

# ============================================================
# 1. SUPPRIMER LES MIGRATIONS PAR DÉFAUT
# ============================================================
echo "Suppression des migrations par défaut..."
rm -f database/migrations/0001_01_01_000000_create_users_table.php
rm -f database/migrations/0001_01_01_000001_create_cache_table.php
rm -f database/migrations/0001_01_01_000002_create_jobs_table.php
rm -f database/migrations/2026_06_23_*_create_personal_access_tokens_table.php 2>/dev/null

# ============================================================
# 2. CRÉER LES MIGRATIONS
# ============================================================
echo "Création des migrations..."

cat > database/migrations/2024_01_01_000001_create_agences_table.php << 'EOF'
<?php
use Illuminate\Database\Migrations\Migration;
use Illuminate\Database\Schema\Blueprint;
use Illuminate\Support\Facades\Schema;

return new class extends Migration {
    public function up()
    {
        Schema::create('agences', function (Blueprint $table) {
            $table->id();
            $table->string('code', 20)->unique();
            $table->string('nom', 100);
            $table->string('adresse')->nullable();
            $table->string('telephone', 30)->nullable();
            $table->string('email', 100)->nullable();
            $table->string('responsable', 100)->nullable();
            $table->string('devise', 10)->default('GNF');
            $table->decimal('solde_cache', 15, 2)->default(0);
            $table->boolean('actif')->default(true);
            $table->timestamps();
            $table->index('code');
        });
    }
    public function down() { Schema::dropIfExists('agences'); }
};
